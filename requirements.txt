# API server first (needed for deploy; Render must have uvicorn in the runtime env)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# Full workflow: LLM, LangGraph, email
langchain-core>=0.2.38
langchain>=0.2.16
langchain-groq>=0.1.9
langgraph>=0.2.14

# Database & data
aiosqlite>=0.20.0
openpyxl>=3.1.5

# Email (SMTP send)
aiosmtplib>=3.0.1

# Environment & logging
python-dotenv>=1.0.1
rich>=13.7.1
tenacity>=8.5.0
